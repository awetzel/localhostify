{
  "sockets": {
    "tcp" : { "connect": "*" },
    "tcpServer" : { "listen": ["127.0.0.1","127.0.0.1"] }
  },
  "app": {
      "background": { "scripts": ["background.js"] }
  },
  "manifest_version": 2,
  "permissions": ["storage"],
  "icons": { "48": "logo48.png", "128": "logo128.png" },
  "description": "Simple chrome extension which take a table : port -> fqdn and create for each record a server listening on localhost:[port] and transfer sockets to [fqdn].",
  "name": "Localhostify",
  "version": "0.0.1"
}
